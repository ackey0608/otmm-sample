# otmm-sample
study for otmm

```
# Use case: Normal
$ curl -X POST http://localhost:8080/start-lra/OK

# Use case: Participant1 is not working well
$ curl -X POST http://localhost:8080/start-lra/error1st

# Use case: Participant2 is not working well
$ curl -X POST http://localhost:8080/start-lra/error2nd
```
